$kind: http-request
method: PATCH
url: '{{baseUrl}}/landlord/requests/{{rentalRequestId}}'
order: 3000
headers:
  - key: Authorization
    value: 'Bearer {{token}}'
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "status": "APPROVED"
    }
